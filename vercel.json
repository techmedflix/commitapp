{
  "buildCommand": "yarn db:migrate && yarn build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/api", "destination": "/api" },
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
